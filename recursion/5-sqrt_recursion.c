#include "main.h"
int
_sqrt_recursion(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	else if (n == 0)
	{
		return (0);
	}
	return (_sqrt_recursion(n - 1));

	{
	int a, s;

	if (s * s < a)
	{
	return (-1);
	}
}
}
